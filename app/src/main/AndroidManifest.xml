<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Puhelun käsittely ja lokitiedot (vaaditaan roolin tueksi) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Android 13+ ilmoitukset -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallBlocker">

        <!-- Pääikkuna -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Numerolistat -->
        <activity android:name=".BlockedNumbersActivity" />
        <activity android:name=".AllowedNumbersActivity" />
        <activity android:name=".AllowedCountriesActivity" />
        <activity android:name=".BlockedCountriesActivity" />

        <!-- Lokit -->
        <activity android:name=".LogActivity" />
        <activity android:name=".SpamReportActivity" />

        <!-- 🔥 KORJATTU: Androidin virallinen puhelujen suodatuspalvelu -->
        <service
            android:name=".CallBlockerService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

    </application>
</manifest>